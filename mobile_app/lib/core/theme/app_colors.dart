import 'package:flutter/material.dart';

abstract class AppColors {
  // Background (Auth Screens Gradient)
  static const Color bgTop = Color(0xFFF6F1FF);
  static const Color bgBottom = Color(0xFFB68CFF);

  // Brand Colors
  static const Color primary = Color(0xFF2B0A4A);

  // Text Colors
  static const Color textDark = Color(0xFF1B1026);

  // Borders
  static const Color border = Color(0xFF2B0A4A);

  // Inputs
  static const Color fieldFill = Color(0xFFF4EDFF);

  // Common
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}